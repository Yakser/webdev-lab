import React from 'react';
import LoginForm from "@/app/components/LoginForm";

const Page = () => {
    return (
        <section>
            <h2 className={'title'}>Вход</h2>
            <LoginForm/>

        </section>
    );
};

export default Page;